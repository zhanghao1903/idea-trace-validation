# PR Review: #8 at `45527cb87713`

- Decision: **REQUEST_CHANGES**
- Review mode: read-only
- Base: `codex/v0-1-project-plan` at `99734e8e6c456e0366d28ce7d9a731b3544f0a56`
- Head: `codex/lp-05-deploy-rollback-hotfix` at `45527cb877130a7a10de434805729c79886f01b3`
- Reviewed at: `2026-08-04T13:00:16Z`

## Outcome

Cycle 8 resolves PRR-005 for its original same-project trigger. Persisted PASS and NOT_APPLICABLE cleanup results are now re-observed before lifecycle or terminal writes, and committed regressions correctly reject restore/production resource reappearance and preserved-upgrade resource-set drift. The 94-test deployment unit suite, authority and restore-chain suites, typecheck, format, lint, static readiness, and both exact-head GitHub checks pass.

Approval remains withheld for one new recovery-scope defect.

## Blocking finding

### PRR-006 — Recovery observes a mutable production project instead of the immutable target (`S2`)

`finalizePersistedRollback` passes `input.productionProject` to the new reconciliation gate. The host producer supplies that value from `config().composeProject`, which is loaded from current process environment. The attempt and persisted rollback aggregate already bind the immutable target project, but the gate does not derive from or compare against that authority.

A review-only regression persisted a fresh-attempt aggregate for project A with lifecycle-null production `NOT_APPLICABLE`, crashed before terminal evidence, recreated an exact-owned container under A, then retried with current configuration project B. Recovery observed empty B and returned terminal `ROLLED_BACK`; the exact-owned A container remained. A fresh-context reviewer independently reproduced the same false success with one application callback total and zero additional deletes.

Required remediation: derive the production reconciliation project from `attempt.target.composeProject` and verified `evidence.composeProject`, or reject any supplied mismatch before observation. The host active rollback path should pass immutable target authority. Add lifecycle-null configuration-drift coverage requiring no terminal success, no repeated callback/delete, and deterministic rejection before observing B.

## Validation

- Contracts build and `typecheck:lp05`: PASS.
- `format:check`, `lint`, and `git diff --check`: PASS.
- `test:deployment:unit`: 7 files / 94 tests PASS.
- `test:deployment:authority`: 16 tests PASS.
- `test:deployment:restore-chain`: 27 tests PASS.
- Static release readiness: `LP05_RELEASE_READINESS_PASS`.
- Review-only lifecycle-null project-drift replay: reproduced PRR-006.
- Fresh-context independent review: independently reproduced PRR-006.
- GitHub Actions run `30908997716`: `verify` job `91990766656` and `lp05-candidate` job `91991475076` both succeeded for the exact head.
- PR snapshot: OPEN, non-draft, MERGEABLE/CLEAN, exact base and head unchanged at observation time.

## Boundary

No production host, DNS, public endpoint, real secret, deployment, merge, release, or publication was touched. Engineering Review remains review-only.
