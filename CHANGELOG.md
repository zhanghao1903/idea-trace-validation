# Changelog

## Unreleased

### Docs

- Define the reviewed plan for splitting v0.1 into eight independently authorized implementation features with one project-management entry.
- Add the single v0.1 project-management entry and eight Draft implementation plans, with independent lifecycle, dependency, verification, and acceptance boundaries.
- Require exact commit-bound lifecycle authority bundles and parent/head transition evidence before any implementation plan can advance beyond Draft.
- Treat lifecycle bundles as cache-only projections and require canonical lifecycle plus live GitHub resolution, CommonMark-accurate fences, and terminal credential-key detection before any authority-driven state can pass.
- Remove production lifecycle-state overrides; isolate fixture authority in a test-only build and reject symlink, hardlink, and tracked-inode authority aliases before path normalization.
- Derive all seven plan statuses and sixteen role-bound transitions from one exhaustive authority registry, including Deferred recovery and versioned Accepted-to-Draft supersession.
- Paginate every GitHub authority collection to completion, bind every page into the authority digest, and reject inconsistent totals, duplicates, or impossible page boundaries.
- Scan scalar, array, object, and multiline values under sensitive terminal keys with redacted diagnostics.
- Normalize JSON Unicode escapes and derive hierarchical structured-key paths before secret classification, rejecting nested API/database credentials without exposing values.
- Bind plan projections to canonical Engineering Lifecycle config schema 1 and state schema 2, accept only workflowctl-supported routed messages, and require exact per-edge source/target stages.
- Carry user or Requirements defer/reopen/recovery authority in immutable repository-owner GitHub decisions, with RequirementsHandoff binding and explicit Deferred recovery continuity.
- Continuously validate reopened Draft authority and Superseded history, requiring synchronized plan/portfolio ChangeRecords whenever lifecycle evidence changes.
- Resolve canonical lifecycle state with workflowctl-compatible `CODEX_HOME` semantics and validate full config/state, queue, GoalRun, dispatch, and stage invariants.
- Generate lifecycle transition evidence through real workflowctl operations, enforce the approved per-edge role routes, and validate every canonical state before checker execution.
- Make the original Accepted-to-Draft supersession authority immutable across later Draft commits, including actor, time, reason, commit, decision, and message lineage.
- Recursively reject flow-style structured credentials and plural sensitive containers such as `apiKeys`, while keeping diagnostics value-redacted.
