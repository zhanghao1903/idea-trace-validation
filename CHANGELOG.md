# Changelog

## Unreleased

### Docs

- Define the reviewed plan for splitting v0.1 into eight independently authorized implementation features with one project-management entry.
- Add the single v0.1 project-management entry and eight Draft implementation plans, with independent lifecycle, dependency, verification, and acceptance boundaries.
- Require exact commit-bound lifecycle authority bundles and parent/head transition evidence before any implementation plan can advance beyond Draft.
- Treat lifecycle bundles as cache-only projections and require canonical lifecycle plus live GitHub resolution, CommonMark-accurate fences, and terminal credential-key detection before any non-Draft state can pass.
- Remove production lifecycle-state overrides; isolate fixture authority in a test-only build and reject symlink, hardlink, and tracked-inode authority aliases before path normalization.
- Derive all seven plan statuses and sixteen role-bound transitions from one exhaustive authority registry, including Deferred recovery and versioned Accepted-to-Draft supersession.
- Paginate every GitHub authority collection to completion, bind every page into the authority digest, and reject inconsistent totals, duplicates, or impossible page boundaries.
- Scan scalar, array, object, and multiline values under sensitive terminal keys with redacted diagnostics.
